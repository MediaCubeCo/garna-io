<div class="hero-console z-10 rounded-[24px]"
                aria-label="Payroll dashboard preview" data-translate-aria-label="heroVisual.ariaLabel">
                <div class="relative z-10 p-4 md:p-5">
                    <div class="flex items-start justify-between gap-4">
                        <div>
                            <h2 class="text-xl font-semibold tracking-tight text-white font-manrope" data-translate="heroVisual.title">
                                Global payroll run
                            </h2>
                            <p class="mt-1.5 max-w-xs text-xs leading-5 text-gray-400 font-manrope" data-translate="heroVisual.subtitle">
                                June 2026 batch across 9 countries, employees, contractors, and local tax rules
                            </p>
                        </div>
                        <div class="hero-ring shrink-0">
                            <span class="relative z-10 text-sm font-semibold text-white font-manrope">92%</span>
                        </div>
                    </div>

                    <div class="hero-run-card mt-3 rounded-2xl border border-white/10 p-3.5">
                        <div class="relative z-10 flex items-start justify-between gap-4">
                            <div>
                                <div class="text-xs font-semibold uppercase tracking-[0.16em] text-gray-500" data-translate="heroVisual.readyToRelease">
                                    Ready to release
                                </div>
                                <div class="mt-1.5 text-3xl font-semibold tracking-tight text-white font-manrope">
                                    $79,940
                                </div>
                                <div class="mt-1 text-xs text-gray-500" data-translate="heroVisual.peopleSummary">12 employees · 12 contractors</div>
                            </div>
                            <button type="button"
                                class="inline-flex h-10 items-center justify-center gap-2 rounded-xl border border-white/15 bg-white/90 px-3.5 text-sm font-medium text-[#071000] shadow-[0_16px_34px_-24px_rgba(255,255,255,0.72)] transition-colors hover:bg-white font-manrope">
                                <span class="text-lg leading-none">+</span>
                                <span data-translate="heroVisual.release">Release</span>
                            </button>
                        </div>
                        <div class="relative z-10 mt-3 flex items-center justify-between text-xs text-gray-500">
                            <span data-translate="heroVisual.fundingAllocated">Funding allocated</span>
                            <span class="font-semibold text-[#CBF300]">92%</span>
                        </div>
                        <div class="hero-progress relative z-10 mt-2"><span></span></div>
                        <div class="relative z-10 mt-3 grid grid-cols-3 gap-2">
                            <div class="rounded-xl border border-white/10 bg-black/25 p-2.5">
                                <div class="text-xs text-gray-500" data-translate="heroVisual.salaries">Salaries</div>
                                <div class="mt-1 text-base font-semibold text-white font-manrope">$48.6k</div>
                            </div>
                            <div class="rounded-xl border border-white/10 bg-black/25 p-2.5">
                                <div class="text-xs text-gray-500" data-translate="heroVisual.contractors">Contractors</div>
                                <div class="mt-1 text-base font-semibold text-white font-manrope">$31.3k</div>
                            </div>
                            <div class="rounded-xl border border-white/10 bg-black/25 p-2.5">
                                <div class="text-xs text-gray-500" data-translate="heroVisual.taxReserve">Tax reserve</div>
                                <div class="mt-1 text-base font-semibold text-[#CBF300] font-manrope">$8.2k</div>
                            </div>
                        </div>

                        <div class="dashboard-soft-line relative z-10 my-2.5"></div>

                        <div class="relative z-10 grid gap-1.5">
                            <div class="dashboard-country-row grid grid-cols-[auto_auto_minmax(0,1fr)_auto] items-center gap-2.5 overflow-hidden rounded-xl border border-white/10 bg-black/20 p-2"
                                style="--row-delay: 260ms;">
                                <span class="dashboard-country-code">US</span>
                                <span class="dashboard-avatar" aria-hidden="true">
                                    <img src="https://hoirqrkdgbmvpwutwuwj.supabase.co/storage/v1/object/public/assets/assets/25df7579-09f7-4c8c-bb39-18a974a980d0_320w.webp"
                                        alt="" loading="eager" decoding="async">
                                </span>
                                <div class="min-w-0">
                                    <div class="truncate text-xs font-semibold text-white font-manrope" data-translate="heroVisual.alexRole">Alex · contractor</div>
                                    <div class="truncate text-xs text-gray-500" data-translate="heroVisual.swiftInvoice">SWIFT · invoice auto-issued</div>
                                </div>
                                <div class="text-right">
                                    <div class="text-xs font-semibold text-white font-manrope">$4.8k</div>
                                    <div class="text-[11px] text-[#CBF300]" data-translate="heroVisual.ready">Ready</div>
                                </div>
                            </div>
                            <div class="dashboard-country-row grid grid-cols-[auto_auto_minmax(0,1fr)_auto] items-center gap-2.5 overflow-hidden rounded-xl border border-white/10 bg-black/20 p-2"
                                style="--row-delay: 340ms;">
                                <span class="dashboard-country-code">PL</span>
                                <span class="dashboard-avatar" aria-hidden="true">
                                    <img src="https://hoirqrkdgbmvpwutwuwj.supabase.co/storage/v1/object/public/assets/assets/6e825b87-0682-49e3-98c2-dbdab1f8f9c5_320w.webp"
                                        alt="" loading="eager" decoding="async">
                                </span>
                                <div class="min-w-0">
                                    <div class="truncate text-xs font-semibold text-white font-manrope" data-translate="heroVisual.mayaRole">Maya · employee</div>
                                    <div class="truncate text-xs text-gray-500" data-translate="heroVisual.sepaBenefits">SEPA · benefits included</div>
                                </div>
                                <div class="text-right">
                                    <div class="text-xs font-semibold text-white font-manrope">€3.9k</div>
                                    <div class="text-[11px] text-[#CBF300]" data-translate="heroVisual.taxOk">Tax OK</div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </div>

<style>
    .hero-console {
        position: relative;
        width: 100%;
        max-width: 520px;
        margin: 3rem auto 0;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.08);
        background:
            radial-gradient(circle at 78% 16%, rgba(203, 243, 0, 0.16), transparent 28%),
            radial-gradient(circle at 18% 92%, rgba(24, 89, 64, 0.22), transparent 32%),
            linear-gradient(180deg, rgba(10, 10, 10, 0.98), rgba(5, 5, 5, 0.98));
        box-shadow:
            0 28px 86px -34px rgba(203, 243, 0, 0.65),
            0 30px 80px -24px rgba(0, 0, 0, 0.85);
        animation:
            dashboard-enter 900ms cubic-bezier(0.16, 1, 0.3, 1) both,
            dashboard-float 7s ease-in-out 1s infinite;
    }

    .hero-console::before {
        content: '';
        position: absolute;
        inset: 0;
        pointer-events: none;
        background:
            linear-gradient(to right, rgba(255, 255, 255, 0.035) 1px, transparent 1px),
            linear-gradient(to bottom, rgba(255, 255, 255, 0.035) 1px, transparent 1px);
        background-size: 38px 38px;
        mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.78), transparent 72%);
        opacity: 0.72;
    }

    .hero-console::after {
        content: '';
        position: absolute;
        top: 0;
        bottom: 0;
        left: -32%;
        width: 24%;
        pointer-events: none;
        background: linear-gradient(90deg, transparent, rgba(203, 243, 0, 0.08), transparent);
        transform: skewX(-16deg);
        animation: dashboard-sweep 5.8s ease-in-out 1.2s infinite;
    }

    .hero-run-card {
        position: relative;
        overflow: hidden;
        background:
            radial-gradient(circle at 84% 18%, rgba(203, 243, 0, 0.16), transparent 28%),
            linear-gradient(145deg, rgba(255, 255, 255, 0.058), rgba(255, 255, 255, 0.018));
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.045);
    }

    .hero-run-card::before {
        content: '';
        position: absolute;
        inset: -35% 48% 20% -20%;
        pointer-events: none;
        background: radial-gradient(circle, rgba(203, 243, 0, 0.2), transparent 58%);
        filter: blur(18px);
        opacity: 0.7;
    }

    .hero-ring {
        position: relative;
        display: grid;
        width: 3.6rem;
        height: 3.6rem;
        place-items: center;
        border-radius: 999px;
        background: conic-gradient(from 210deg, #CBF300 0 92%, rgba(255, 255, 255, 0.1) 92% 100%);
        box-shadow: 0 0 34px -12px rgba(203, 243, 0, 0.85);
    }

    .hero-ring::before {
        content: '';
        position: absolute;
        inset: 6px;
        border-radius: inherit;
        background: #070707;
    }

    .hero-ring::after {
        content: '';
        position: absolute;
        inset: -5px;
        border: 1px solid rgba(203, 243, 0, 0.26);
        border-radius: inherit;
        animation: dashboard-pulse 2.8s ease-in-out infinite;
    }

    .hero-progress {
        height: 0.55rem;
        overflow: hidden;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.08);
    }

    .hero-progress span {
        display: block;
        width: 92%;
        height: 100%;
        border-radius: inherit;
        background: #CBF300;
        box-shadow: 0 0 24px -7px rgba(203, 243, 0, 0.95);
        animation: hero-progress 2.4s cubic-bezier(0.16, 1, 0.3, 1) both;
    }

    .dashboard-country-row {
        opacity: 0;
        transform: translateY(12px);
        animation: dashboard-item-in 620ms cubic-bezier(0.16, 1, 0.3, 1) both;
        animation-delay: var(--row-delay, 0ms);
    }

    .dashboard-country-row:hover {
        border-color: rgba(203, 243, 0, 0.22);
        background: rgba(255, 255, 255, 0.045);
    }

    .dashboard-country-code {
        display: grid;
        width: 1.65rem;
        height: 1.65rem;
        place-items: center;
        border-radius: 0.6rem;
        background: rgba(203, 243, 0, 0.14);
        color: #CBF300;
        font-size: 0.62rem;
        font-weight: 800;
        letter-spacing: 0.03em;
    }

    .dashboard-avatar {
        position: relative;
        display: block;
        width: 2rem;
        height: 2rem;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.13);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.08);
        box-shadow: 0 10px 24px -16px rgba(0, 0, 0, 0.9);
    }

    .dashboard-avatar img {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: cover;
        filter: saturate(0.86) contrast(1.04) brightness(0.92);
        transform: scale(1.02);
    }

    .dashboard-avatar::after {
        content: '';
        position: absolute;
        inset: 0;
        pointer-events: none;
        background: radial-gradient(circle at 35% 25%, rgba(255, 255, 255, 0.46), transparent 28%);
        opacity: 0.34;
    }

    .dashboard-soft-line {
        height: 1px;
        background: linear-gradient(90deg, transparent, rgba(203, 243, 0, 0.34), transparent);
    }

    :global(html[data-theme="light"]) .hero-console {
        border-color: transparent;
        background:
            radial-gradient(circle at 78% 16%, rgba(203, 243, 0, 0.2), transparent 30%),
            radial-gradient(circle at 18% 92%, rgba(24, 89, 64, 0.1), transparent 34%),
            #ffffff;
        box-shadow: 0 30px 76px -46px rgba(31, 38, 25, 0.34);
    }

    :global(html[data-theme="light"]) .hero-console::before {
        background:
            linear-gradient(to right, rgba(16, 16, 16, 0.035) 1px, transparent 1px),
            linear-gradient(to bottom, rgba(16, 16, 16, 0.035) 1px, transparent 1px);
        opacity: 0.42;
    }

    :global(html[data-theme="light"]) .hero-run-card {
        border-color: transparent;
        background:
            radial-gradient(circle at 84% 18%, rgba(203, 243, 0, 0.18), transparent 30%),
            #f6f7f2;
        box-shadow: 0 16px 36px -30px rgba(31, 38, 25, 0.3);
    }

    :global(html[data-theme="light"]) .hero-ring::before {
        background: #ffffff;
    }

    :global(html[data-theme="light"]) .hero-progress {
        background: rgba(16, 16, 16, 0.1);
    }

    :global(html[data-theme="light"]) .dashboard-avatar {
        border-color: rgba(16, 16, 16, 0.1);
        background: #eef0ea;
        box-shadow: 0 12px 24px -18px rgba(31, 38, 25, 0.28);
    }

    @media (min-width: 1024px) {
        .hero-console {
            position: absolute;
            top: clamp(11.5rem, calc(13vw + 4rem), 15rem);
            right: max(var(--garna-container-gutter), calc((100vw - var(--garna-container-content-max)) / 2));
            width: min(42vw, 520px);
            margin: 0;
        }
    }

    @media (min-width: 1024px) and (max-width: 1200px) {
        .hero-console {
            width: min(42vw, 500px);
        }
    }

    @keyframes dashboard-enter {
        from { opacity: 0; filter: blur(14px); }
        to { opacity: 1; filter: blur(0); }
    }

    @keyframes dashboard-float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }

    @keyframes dashboard-sweep {
        0%, 52% { left: -32%; opacity: 0; }
        62% { opacity: 1; }
        82%, 100% { left: 110%; opacity: 0; }
    }

    @keyframes dashboard-item-in {
        to { opacity: 1; transform: translateY(0); }
    }

    @keyframes dashboard-pulse {
        0%, 100% { box-shadow: 0 0 0 0 rgba(203, 243, 0, 0.22); }
        50% { box-shadow: 0 0 0 7px rgba(203, 243, 0, 0); }
    }

    @keyframes hero-progress {
        from { width: 18%; }
        to { width: 92%; }
    }

    @media (prefers-reduced-motion: reduce) {
        .hero-console,
        .hero-console::after,
        .hero-ring::after,
        .hero-progress span,
        .dashboard-country-row {
            animation: none;
        }

        .dashboard-country-row {
            opacity: 1;
            transform: none;
        }
    }
</style>
