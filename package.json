{
	"name": "garna-io",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "cross-env ASTRO_TELEMETRY_DISABLED=1 astro build",
		"deploy": "npm run build && wrangler deploy",
		"dev": "npm run build && wrangler dev",
		"start": "npm run dev",
		"astro:dev": "cross-env ASTRO_TELEMETRY_DISABLED=1 astro dev",
		"test": "vitest run",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.34",
		"@cloudflare/workers-types": "^4.20250614.0",
		"astro": "^6.4.4",
		"cross-env": "7.0.3",
		"typescript": "^5.5.2",
		"vitest": "~3.0.7",
		"wrangler": "^4.59.2"
	}
}
