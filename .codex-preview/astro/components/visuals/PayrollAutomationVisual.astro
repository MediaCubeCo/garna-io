<div class="payroll-flow mx-auto w-full max-w-[410px] text-left font-sans">
	<div class="flow-row">
		<span class="flow-icon flow-icon--blue">
			<svg viewBox="0 0 24 24"><path d="M4 7h16v11H4zM8 4v6m8-6v6M4 11h16" /></svg>
		</span>
		<div class="flow-card flow-card--primary"><span class="text-gray-500">When</span><strong> Payroll period is ready</strong></div>
	</div>

	<div class="flow-connector"></div>

	<div class="flow-row">
		<span class="flow-icon">
			<svg viewBox="0 0 24 24"><path d="M6 5h12M6 12h8M6 19h12"/><circle cx="18" cy="12" r="2" /></svg>
		</span>
		<div class="flow-sentence"><span>If</span><b>24 timesheets</b><span>are approved</span></div>
	</div>

	<div class="flow-connector flow-connector--turn"></div>

	<div class="flow-row flow-row--indented">
		<span class="flow-icon">
			<svg viewBox="0 0 24 24"><path d="M4 12h16M12 4v16M7 7l10 10M17 7 7 17" /></svg>
		</span>
		<span class="flow-label">Calculate</span>
		<div class="flow-card"><span class="initials initials--green">AU</span><strong>Salaries & taxes</strong></div>
	</div>

	<div class="flow-connector flow-connector--short"></div>

	<div class="flow-row flow-row--indented">
		<span class="flow-icon">
			<svg viewBox="0 0 24 24"><path d="M5 12l4 4L19 6" /></svg>
		</span>
		<span class="flow-label">Require</span>
		<div class="flow-card"><span class="initials initials--violet">FO</span><strong>Finance approval</strong></div>
	</div>

	<div class="flow-connector flow-connector--short"></div>

	<div class="flow-row flow-row--indented">
		<span class="flow-icon">
			<svg viewBox="0 0 24 24"><path d="M5 7h14v11H5zM8 4v6m8-6v6" /></svg>
		</span>
		<span class="flow-label">Schedule</span>
		<div class="flow-card"><span class="initials initials--lime">28</span><strong>Payout · 09:00</strong></div>
	</div>
</div>

<style>
	.payroll-flow { --line: rgb(255 255 255 / .13); padding: .25rem .5rem; }
	.flow-row { position: relative; display: flex; min-height: 3rem; align-items: center; gap: .65rem; }
	.flow-row--indented { padding-left: 2.5rem; }
	.flow-icon { position: relative; z-index: 2; display: flex; height: 2rem; width: 2rem; flex: 0 0 auto; align-items: center; justify-content: center; border: 1px solid rgb(255 255 255 / .08); border-radius: .55rem; background: #1a1a1a; color: rgb(156 163 175); }
	.flow-icon--blue { background: #1b4fff; color: white; }
	.flow-icon svg { height: 1rem; width: 1rem; fill: none; stroke: currentColor; stroke-width: 1.6; stroke-linecap: round; stroke-linejoin: round; }
	.flow-card { display: flex; min-height: 3rem; min-width: 0; align-items: center; gap: .65rem; border: 1px solid rgb(255 255 255 / .08); border-radius: .8rem; background: #0a0a0a; padding: .55rem .75rem; box-shadow: 0 12px 25px rgb(0 0 0 / .22); }
	.flow-card--primary { padding-inline: 1rem; }
	.flow-card strong { color: white; font-size: .72rem; font-weight: 500; white-space: nowrap; }
	.flow-card--primary span { margin-right: .15rem; font-size: .68rem; }
	.flow-sentence { display: flex; min-width: 0; align-items: center; gap: .45rem; color: rgb(209 213 219); font-size: .7rem; white-space: nowrap; }
	.flow-sentence b { border-radius: .65rem; background: #0a0a0a; padding: .65rem .75rem; color: white; font-weight: 500; }
	.flow-label { width: 3.25rem; flex: 0 0 auto; color: rgb(156 163 175); font-size: .68rem; }
	.initials { display: flex; height: 1.9rem; width: 1.9rem; flex: 0 0 auto; align-items: center; justify-content: center; border-radius: .5rem; font-size: .62rem; font-weight: 600; }
	.initials--green { background: rgb(34 197 94 / .12); color: rgb(134 239 172); }
	.initials--violet { background: rgb(99 102 241 / .14); color: rgb(165 180 252); }
	.initials--lime { background: rgb(203 243 0 / .12); color: #CBF300; }
	.flow-connector { height: 1.05rem; width: 1px; margin-left: .98rem; background: var(--line); }
	.flow-connector--turn { height: 1.2rem; margin-left: .98rem; }
	.flow-connector--short { margin-left: 3.48rem; }
	@media (max-width: 480px) { .payroll-flow { transform: scale(.9); } }
</style>
